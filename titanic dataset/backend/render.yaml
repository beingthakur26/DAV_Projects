services:
  - type: web
    name: titanic-survival-api
    env: python
    rootDir: backend
    buildCommand: python -m pip install --upgrade pip setuptools && pip install -r requirements.txt
    startCommand: gunicorn app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
